server:
  port: 8084
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/angelo04/microservicios-01
          search-paths: config-data
  application:
    name: config-service
